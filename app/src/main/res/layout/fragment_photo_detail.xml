<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".NavigationSection.PhotoDetailFragment">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:srcCompat="@mipmap/ic_launcher"
        android:layout_centerHorizontal="true"
        android:id="@+id/image_item"
        android:layout_marginTop="40px"
        android:padding="5dp"
        android:background="@android:color/holo_green_light" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:text="Логин:"
            android:layout_weight="35"
            android:layout_height="match_parent"
            android:layout_width="0dp"
            android:textSize="20sp"
            android:gravity="center" />

        <EditText
            android:layout_weight="80"
            android:id="@+id/face_detail_short_name"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:inputType="textPersonName"
            android:layout_below="@id/face_detail_face"
            android:text="Name"
            android:textSize="20sp" />
    </LinearLayout>

    <LinearLayout
        android:layout_height="wrap_content"
        android:layout_width="match_parent">

        <Button
            android:id="@+id/face_detail_save_button"
            android:text="@string/save_button_text"
            android:layout_width="0dp"
            android:layout_weight="50"
            android:layout_height="wrap_content" />

        <Button
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:layout_weight="50"
            android:text="Сделать главной"
            android:id="@+id/make_main_button" />

        <Button
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:layout_weight="50"
            android:text="Удалить"
            android:id="@+id/delete_photo_button" />

    </LinearLayout>

    <Button
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:text="Добавить на сервер"
        android:id="@+id/add_photo_to_server_button" />
</LinearLayout>
